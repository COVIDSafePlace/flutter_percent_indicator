name: percent_indicator_nullsafe
description: Updated for Flutter dart null safety. Library that allows you to display progress widgets based on percentage, can be Circular or Linear, you can also customize it to your needs.
version: 3.1.0
homepage: https://github.com/COVIDSafePlace/flutter_percent_indicator_nullsafe
repository: https://github.com/COVIDSafePlace/flutter_percent_indicator_nullsafe

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: 1.5.1

flutter:
